import 'package:flutter/material.dart';

class Account_Fragment extends StatelessWidget {
  const Account_Fragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
